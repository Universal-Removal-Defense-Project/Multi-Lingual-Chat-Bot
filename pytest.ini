[pytest]
# Add the repo root to sys.path so tests can `import backend`, `import ui`, etc.
# regardless of how pytest is invoked (bare `pytest` vs `python -m pytest`).
pythonpath = .
testpaths = tests
